@model IEnumerable<WebApplication5.Models.Rekening>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Klant.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.RekeningNaam)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Beschrijving)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Saldo)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Klant.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.RekeningNaam)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Beschrijving)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Saldo)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.RekeningId }) |
            @Html.ActionLink("Details", "Details", new { id=item.RekeningId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.RekeningId })
        </td>
    </tr>
}

</table>
